# Frontend

This folder is reserved for the React UI.

Suggested stack:
- React
- Vite
- Monaco Editor or CodeMirror
- terminal-style output panel

The frontend will let users write code, save files, and execute programs through the backend API.
